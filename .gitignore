/target
.superpowers/
.claude/
.DS_Store
*.tailored.epub
*.x4.epub
*.x3.epub
.idea/
.vscode/
plans/
CLAUDE.md
AGENTS.md

# release build artifacts (packaged locally while testing release.yml)
/*.tar.gz
/*.zip
/SHA256SUMS

# desktop app (app/)
app/node_modules/
app/dist/
app/src-tauri/binaries/*
!app/src-tauri/binaries/.gitkeep
app/src-tauri/gen/

# updater signing keys. The PUBLIC key belongs in tauri.conf.json (and is in
# there); the private key and its password are GitHub secrets and must never
# land in the repo, not even for a minute.
*.key
*.key.pub
